__pycache__/
*.py[cod]
.venv/
.DS_Store
.pytest_cache/

# Verified on export but too large for git (10,029,776 nodes; 130 MB compressed)
models/random_forest.joblib

# SQLite is a derived cache rebuilt from the JSONL store; never tracked
*.db
data/local/

# _site is committed on purpose: Cloudflare Pages serves it with no build step
